extends layout

block content
  h1.
    User List
  ul
    each user, i in userlist
      li
        = user.name 
      //li = user.type + ' ' + user.name
      
      //a(href="mailto:#{user.type}")= user.name